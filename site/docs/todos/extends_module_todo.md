---
title: "TODO: Extends module"
discard: true
---

# TODO: Extends module

- Add regression tests for `extend "owner/repo@tag"` vs latest `main` behavior.
- Ensure cache invalidation policy for “latest main” is clear, deterministic, and documented.
- Add explicit cycle detection / recursion guard for extension `config.scriban` chains.
- Document and test private/public install behavior across OSes.

